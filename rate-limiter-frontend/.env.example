# URL of the backend from ../rate-limiter (npm start or docker compose up).
# The live demo panel also lets you override this at runtime in the UI.
NEXT_PUBLIC_API_URL=http://localhost:3000 || https://rate-limmiter.onrender.com;
